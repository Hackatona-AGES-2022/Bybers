import React, { FC } from "react";
import { Title } from "./style";

interface Props {
  
}
const Example: React.FC<Props> = () => {
  return (
    <Title>
      tyeste
    </Title>
  );
};

export default Example;